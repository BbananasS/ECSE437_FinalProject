import React from "react"

const Section = ({ children }) => {  // eslint-disable-line
    return (
        <div style={{ margin: "50px" }}>
            {children}
        </div>

    )
}; 

export default Section